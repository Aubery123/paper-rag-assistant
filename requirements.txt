# ---- 文档解析 ----
pymupdf>=1.24            # PDF 解析，保留页码/章节

# ---- 向量库 ----
qdrant-client>=1.9       # 向量检索；P2 混合检索用 Qdrant 原生 BM25

# ---- 阿里百炼 API（LLM 作答 + 文本嵌入 + 重排，OpenAI 兼容接口）----
openai>=1.30             # 统一走 dashscope compatible-mode 端点

# ---- 服务 ----
fastapi>=0.111
uvicorn[standard]>=0.30
sse-starlette>=2.1       # SSE 流式
streamlit>=1.35

# ---- 配置 ----
pydantic>=2.7
pydantic-settings>=2.3
python-dotenv>=1.0

# ---- 评测 ----
ragas>=0.1               # RAGAS 风格指标

# ---- 测试 ----
pytest>=8.2
