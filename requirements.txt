# ---- 文档解析 ----
pymupdf>=1.24            # PDF 解析，保留页码/章节

# ---- Embedding / 重排（开源，本地）----
FlagEmbedding>=1.2       # BGE-m3（dense+sparse）/ bge-reranker-v2-m3
sentence-transformers>=3.0

# ---- 向量库 ----
qdrant-client>=1.9       # 向量 + 稀疏(BM25) 混合检索

# ---- LLM（通义千问 qwen-plus，OpenAI 兼容接口）----
openai>=1.30

# ---- 服务 ----
fastapi>=0.111
uvicorn[standard]>=0.30
sse-starlette>=2.1       # SSE 流式
streamlit>=1.35

# ---- 配置 ----
pydantic>=2.7
pydantic-settings>=2.3
python-dotenv>=1.0

# ---- 评测 ----
ragas>=0.1               # RAGAS 风格指标

# ---- 测试 ----
pytest>=8.2
